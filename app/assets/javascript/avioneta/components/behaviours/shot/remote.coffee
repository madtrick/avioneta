define ['avioneta/components/behaviours/shot', 'avioneta/components/behaviours/shot/base'], (Shot, Base) ->
  class Shot.Remote extends Base
    #hit : (model, shot, player) ->
    #  player.hit(remote : false)

